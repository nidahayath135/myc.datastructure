#include <stdio.h>
int main() {
    int arr[] = {1, 2, 3};
    int n = 3;
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++)
            printf("(%d, %d) ", arr[i], arr[j]);
    }
    return 0;
}
