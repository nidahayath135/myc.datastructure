#include <stdio.h>
void printArray(int a[], int n) {
    printf("[");
    for (int i = 0; i < n; i++) {
        printf("%d", a[i]);
        if (i < n - 1) printf(", ");
    }
    printf("]\n");
}

int main() {
    int arr[5] = {1, 2, 3, 4, 5}; 
    printArray(arr, 5);           
    return 0;
}
